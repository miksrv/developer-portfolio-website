<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # Return 404 status and show 404.html
  RewriteRule ^ - [R=404,L]
</IfModule>

# Set up a custom 404 page
ErrorDocument 404 /404.html
